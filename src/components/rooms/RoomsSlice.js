import { createSlice } from "@reduxjs/toolkit";

export const RoomsSlice = createSlice({
    name: 'Rooms',
    initialState: {
        // TODO add initial state for RoomsSlice
    },
    reducers: {},
    extraReducers: {
        // TODO add AsynThunk reducers
    }
});